# IESS 戰情室 Demo

雙擊開啟 `index.html` 即可操作。

目前功能：戰情室 → 維修服務 → 叫修案件（案件列表／新增／編輯／結案）、叫修案件紀錄（日期區間查詢／唯讀明細）。

資料為記憶體假資料，重整後重置。

主要檔案：`index.html`（殼層）、`js/app.js`（三層導覽）、`js/data.js`（假資料與選項）、`js/modules/maintenance.js`（叫修案件）、`js/modules/case-records.js`（叫修案件紀錄）。
